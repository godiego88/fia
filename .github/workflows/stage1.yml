name: FIA Stage 1 (Skeleton)

on:
  workflow_dispatch:     # manual trigger only for now

jobs:
  stage1_skeleton:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Stage 1 placeholder
        run: |
          echo "Stage 1 workflow skeleton created. No logic yet."
